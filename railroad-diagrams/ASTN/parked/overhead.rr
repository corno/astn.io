ComplexDiagram(
    ZeroOrMore(
        Choice(
            0,
            Group(
                Choice(
                    0,
                    Sequence('', Comment('space')),
                    Sequence('', Comment('line feed')),
                    Sequence('', Comment('carriage return')),
                    Sequence('', Comment('horizontal tab')),
                ),
                Comment('JSON Whitespace')
            ),
            Group(
                Sequence(
                    Terminal('//', {cls:'added'}),
                    ZeroOrMore(
                        NonTerminal('Any code point except \\n (line feed) or \\r (carriage return) or control character', {cls:'added'})
                    ),
                ),
                Comment('line comment', {cls:'added'})
            ),
            Group(
                Sequence(
                    Terminal('/*', {cls:'added'}),
                    ZeroOrMore(
                        NonTerminal('Any code point except * followed by / or control character', {cls:'added'}),
                    ),
                    Terminal('*/', {cls:'added'}),
                ),
                Comment('block comment', {cls:'added'})
            )
        ),
        '',
        'skip'
    )
)